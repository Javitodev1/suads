---
import { BaseContainer, Typography, LinkButton, IconCard, Separator } from '@/components'

interface Props {
  id?: string
}

const { id = 'web-design' } = Astro.props
---

<Separator variant={'up_top'} />
<BaseContainer
  variant={'hero'}
  class='bg-neutral-dark space-y-12'
  id={id}>
  <Typography
    class='text-center'
    as={'h2'}
    variant={'title'}
    color={'neutral_white'}>
    <slot name='header'> No module slot header </slot>
  </Typography>

  <div class='flex justify-center'>
    <LinkButton gradient={'accent_1'}>
      <slot name='cta'> No module slot cta </slot>
    </LinkButton>
  </div>

  <div class='grid grid-cols-3 gap-x-12'>
    <slot name="cards" />
  </div>
</BaseContainer>
