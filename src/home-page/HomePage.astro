---
import * as Modules from './modules'
import {
  ServiceCard,
  TestimonyCard,
  SnakeCard,
} from '@/components'
import { AsideLayout } from '@/layouts'
import { Icon, HOME_VIDEOS, HOME_IMAGES } from '@/assets'

const SEO = {
  title: 'Suads | Agencía de Marketing Digital',
  desc: 'Agencía suads te ayuda a desarrollar tu emprendimiento paso a paso para que alcances tus objetivos',
  keywords:
    'diseño web, desarrollo web, ecommerce, social media, instagram, ads, paid media',
}

const hero = {
  src: HOME_VIDEOS.mist,
}

const services = {
  src: [
    HOME_IMAGES.social_media,
    HOME_IMAGES.paid_media,
    HOME_IMAGES.desarrollo_web,
  ],
  alt: [
    'servicio social media',
    'servicio paid media',
    'servicio desarrollo web',
  ],
}

const testimonies = {
  logos: {
    test: '/favicon.svg',
  },
}

const ourmethod = {
  bgSrc: HOME_IMAGES.ourmethod,
}
---

<AsideLayout {...SEO}>
  <Modules.RainbowHero {...hero}>
    <Fragment slot={'body'}> paid media | desarrollo web </Fragment>

    <Fragment slot={'header'}>
      <strong>Conectamos</strong> tu marca con clientes ideales a través de <strong>redes sociales</strong>
    </Fragment>

    <Fragment slot={'cta'}> ¿Cómo lo hacemos? </Fragment>
  </Modules.RainbowHero>

  <Modules.OurMethod {...ourmethod}>
    <Fragment slot={'pre-header'}> Nuestro método </Fragment>

    <Fragment slot={'header'}>
      Esto es lo que podemos <strong>hacer por ti</strong>
    </Fragment>

    <Fragment slot={'cta'}> Te lo explicamos </Fragment>

    <Fragment slot={'cards'}>
      <SnakeCard variant={'left'}>
        <Fragment slot={'header'}>
          <strong>Analizamos</strong> tu negocio
        </Fragment>

        <Fragment slot={'icon'}> <Icon variant={'home_business'} /> </Fragment>
      </SnakeCard>

      <SnakeCard variant={'right'}>
        <Fragment slot={'header'}>
          <strong>Perfilamos</strong> tu cliente ideal
        </Fragment>

        <Fragment slot={'icon'}> <Icon variant={'home_people'} /> </Fragment>
      </SnakeCard>

      <SnakeCard variant={'left'}>
        <Fragment slot={'header'}>
          Creamos una oferta <strong>irresistible</strong>
        </Fragment>

        <Fragment slot={'icon'}> <Icon variant={'home_heart'} /> </Fragment>
      </SnakeCard>

      <SnakeCard variant={'right'}>
        <Fragment slot={'header'}>
          <strong>Segmentamos</strong> los publicos
        </Fragment>

        <Fragment slot={'icon'}> <Icon variant={'home_segment'} /> </Fragment>
      </SnakeCard>

      <SnakeCard variant={'left'}>
        <Fragment slot={'header'}>
          <strong>Creamos un embudo</strong> de ventas
        </Fragment>

        <Fragment slot={'icon'}> <Icon variant={'home_embudo'} /> </Fragment>
      </SnakeCard>

      <SnakeCard variant={'right'}>
        <Fragment slot={'header'}>
          <strong>Optimizamos</strong> la campaña
        </Fragment>

        <Fragment slot={'icon'}> <Icon variant={'home_ad'} /> </Fragment>
      </SnakeCard>

      <SnakeCard variant={'left'}>
        <Fragment slot={'header'}>
          <strong>Desarrollamos</strong> la campaña ganadora
        </Fragment>

        <Fragment slot={'icon'}> <Icon variant={'home_results'} /> </Fragment>
      </SnakeCard>
    </Fragment>
  </Modules.OurMethod>

  <Modules.Services>
    <Fragment slot={'before-header'}>te ayudamos a crecer</Fragment>

    <Fragment slot={'header'}
      >¿qué <strong>soluciones</strong> ofrecemos?</Fragment
    >

    <Fragment slot={'after-header'}>
      ¡Somos los mejores y queremos que tú también lo seas!
      <br />
      El equipo de Suads se enfoca en entregar soluciones personalizadas y resultados
      de calidad.
    </Fragment>

    <Fragment slot={'cards'}>
      <ServiceCard
        src={services.src[0]}
        alt={services.alt[0]}>
        <Fragment slot={'header'}>Social<br />Media</Fragment>
        <Fragment slot={'body'}
          >¡Impulsa el marketing de tus redes sociales!</Fragment
        >
      </ServiceCard>

      <ServiceCard
        src={services.src[1]}
        alt={services.alt[1]}>
        <Fragment slot={'header'}>Paid<br />Media</Fragment>
        <Fragment slot={'body'}
          >¡Impulsa el marketing de tus redes sociales!</Fragment
        >
      </ServiceCard>

      <ServiceCard
        src={services.src[2]}
        alt={services.alt[2]}>
        <Fragment slot={'header'}>Desarrollo<br />Web</Fragment>
        <Fragment slot={'body'}
          >¡Impulsa el marketing de tus redes sociales!</Fragment
        >
      </ServiceCard>
    </Fragment>
  </Modules.Services>

  <Modules.Partners>
    <Fragment slot={'pre-header'}>Nuestros clientes</Fragment>

    <Fragment slot={'header-1'}>¡Súmate a las marcas que <strong>consiguen clientes</strong> con Suads!</Fragment>
    <Fragment slot={'body-1'}></Fragment>

    <Fragment slot={'header-2'}>Queremos trabajar contigo</Fragment>
    <Fragment slot={'body-2'}>Y si tú también quieres conseguir clientes con nosotros empecemos mejor por una primera cita</Fragment>

    <Fragment slot={'cta'}>Hablemos del futuro de tu negocio</Fragment>
  </Modules.Partners>

  <Modules.Testimonies>
    <Fragment slot={'before-header'}>Testimonios</Fragment>
    <Fragment slot={'header'}
      >clientes que <strong>confían</strong> en agencia suads</Fragment
    >
    <Fragment slot={'cards'}>
      <TestimonyCard logoSrc={testimonies.logos.test}>
        <Fragment slot={'body'}
          >lorem ip sum Lorem ipsum dolor sit amet consectetur adipisicing elit.
          Beatae, at! Laudantium aliquam cum vel, sapiente consequatur amet
          doloremque, minima quasi voluptatum recusandae exercitationem
          voluptates voluptatem.</Fragment
        >
        <Fragment slot={'author'}>Javito</Fragment>
        <Fragment slot={'role'}>Senior Devops</Fragment>
      </TestimonyCard>
    </Fragment>
  </Modules.Testimonies>
</AsideLayout>
